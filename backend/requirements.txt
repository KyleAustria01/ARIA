# ARIA Backend Requirements (v3 — clean, no LangGraph)

# --- Web framework ---
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
websockets==12.0
aiofiles==23.2.1

# --- Data validation ---
pydantic==2.6.4
pydantic-settings==2.2.1

# --- Redis ---
redis==5.0.4

# --- PDF parsing ---
PyMuPDF==1.24.2

# --- LLM providers (all via httpx — no vendor SDKs needed) ---
httpx==0.27.0
boto3>=1.34.0          # AWS Bedrock (optional — skipped if keys not set)

# --- STT ---
faster-whisper==1.0.3

# --- TTS ---
edge-tts>=6.1.0

# --- Web search (optional) ---
tavily-python==0.3.0

# --- Testing ---
pytest==8.1.1
pytest-asyncio==0.23.6
